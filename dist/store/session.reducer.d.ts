import { ActionType, SessionDefinitionType } from '../types/session.types';
declare const sessionReducer: (state: SessionDefinitionType | undefined, action: ActionType) => any;
export default sessionReducer;
